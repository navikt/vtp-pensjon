import { XmlformatterPipe } from './xmlformatter.pipe';

describe('XmlformatterPipe', () => {
  it('create an instance', () => {
    const pipe = new XmlformatterPipe();
    expect(pipe).toBeTruthy();
  });
});
